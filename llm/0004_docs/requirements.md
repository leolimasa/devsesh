Read all the markdown files in the `llm` folder to understand was implemented. Then read the existing Go lang source code, and the `web` (frontend) source code. This will give you an overview of what the project is. Then:

* Write a comprehensive README.md file on the project root. Include:
	* What is the project for
	* Quick start
	* Features
	* Usage examples
	* How to build
	* How to test
	* Links to other docs in `doc/`
* Order the README section according to best practices so that it is engaging and easy to read
* Create a `doc/ARCHITECTURE.md` with a high level overview on how the project is structured in an architectural level
