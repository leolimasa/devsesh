Running `devsesh server` starts the server correctly. But there are browser console errors and the page is not displayed.
Figure out a way to test the server endpoint in a browser and make sure the javascript is executed and that it renders correctly. 
You have access to a graphical environment (KDE). Install any needed packages using the existing `flake.nix`.
Keep in mind that the server start will block.
