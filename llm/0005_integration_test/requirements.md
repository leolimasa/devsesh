* Start the Go server with a sqlite db with test data
* Use Playwright to interact with the web ui
* Use the command line client to check that it updates the data in the go server
* Everything should run from `integration_tests.sh`
